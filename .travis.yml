language: r

warnings_are_errors: false 

r_binary_packages:
    - testthat
    - roxygen2

install:    
    - Rscript -e 'roxygen2::roxygenise()'

script:
    - R -e 'devtools::install_deps(dep = T)'
    - R CMD build .
    - R CMD check *tar.gz