# Netherlands Biodiversity Api
# 
# Access to the digitised Natural History collection at the Naturalis Biodiversity Center
# 
# OpenAPI spec version: v2
# Contact: support@naturalis.nl
# Generated by: https://github.com/swagger-api/swagger-codegen.git


#' ScientificName Class
#'
#' @field fullScientificName 
#' @field taxonomicStatus 
#' @field genusOrMonomial 
#' @field subgenus 
#' @field specificEpithet 
#' @field infraspecificEpithet 
#' @field infraspecificMarker 
#' @field nameAddendum 
#' @field authorshipVerbatim 
#' @field author 
#' @field year 
#' @field scientificNameGroup 
#' @field references 
#' @field experts 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ScientificName <- R6::R6Class(
  'ScientificName',
  public = list(
    `fullScientificName` = NULL,
    `taxonomicStatus` = NULL,
    `genusOrMonomial` = NULL,
    `subgenus` = NULL,
    `specificEpithet` = NULL,
    `infraspecificEpithet` = NULL,
    `infraspecificMarker` = NULL,
    `nameAddendum` = NULL,
    `authorshipVerbatim` = NULL,
    `author` = NULL,
    `year` = NULL,
    `scientificNameGroup` = NULL,
    `references` = NULL,
    `experts` = NULL,
    initialize = function(`fullScientificName`, `taxonomicStatus`, `genusOrMonomial`, `subgenus`, `specificEpithet`, `infraspecificEpithet`, `infraspecificMarker`, `nameAddendum`, `authorshipVerbatim`, `author`, `year`, `scientificNameGroup`, `references`, `experts`){
      if (!missing(`fullScientificName`)) {
        stopifnot(is.character(`fullScientificName`), length(`fullScientificName`) == 1)
        self$`fullScientificName` <- `fullScientificName`
      }
      if (!missing(`taxonomicStatus`)) {
        stopifnot(is.character(`taxonomicStatus`), length(`taxonomicStatus`) == 1)
        self$`taxonomicStatus` <- `taxonomicStatus`
      }
      if (!missing(`genusOrMonomial`)) {
        stopifnot(is.character(`genusOrMonomial`), length(`genusOrMonomial`) == 1)
        self$`genusOrMonomial` <- `genusOrMonomial`
      }
      if (!missing(`subgenus`)) {
        stopifnot(is.character(`subgenus`), length(`subgenus`) == 1)
        self$`subgenus` <- `subgenus`
      }
      if (!missing(`specificEpithet`)) {
        stopifnot(is.character(`specificEpithet`), length(`specificEpithet`) == 1)
        self$`specificEpithet` <- `specificEpithet`
      }
      if (!missing(`infraspecificEpithet`)) {
        stopifnot(is.character(`infraspecificEpithet`), length(`infraspecificEpithet`) == 1)
        self$`infraspecificEpithet` <- `infraspecificEpithet`
      }
      if (!missing(`infraspecificMarker`)) {
        stopifnot(is.character(`infraspecificMarker`), length(`infraspecificMarker`) == 1)
        self$`infraspecificMarker` <- `infraspecificMarker`
      }
      if (!missing(`nameAddendum`)) {
        stopifnot(is.character(`nameAddendum`), length(`nameAddendum`) == 1)
        self$`nameAddendum` <- `nameAddendum`
      }
      if (!missing(`authorshipVerbatim`)) {
        stopifnot(is.character(`authorshipVerbatim`), length(`authorshipVerbatim`) == 1)
        self$`authorshipVerbatim` <- `authorshipVerbatim`
      }
      if (!missing(`author`)) {
        stopifnot(is.character(`author`), length(`author`) == 1)
        self$`author` <- `author`
      }
      if (!missing(`year`)) {
        stopifnot(is.character(`year`), length(`year`) == 1)
        self$`year` <- `year`
      }
      if (!missing(`scientificNameGroup`)) {
        stopifnot(is.character(`scientificNameGroup`), length(`scientificNameGroup`) == 1)
        self$`scientificNameGroup` <- `scientificNameGroup`
      }
      if (!missing(`references`)) {
        stopifnot(is.list(`references`), length(`references`) != 0)
        lapply(`references`, function(x) stopifnot(R6::is.R6(x)))
        self$`references` <- `references`
      }
      if (!missing(`experts`)) {
        stopifnot(is.list(`experts`), length(`experts`) != 0)
        lapply(`experts`, function(x) stopifnot(R6::is.R6(x)))
        self$`experts` <- `experts`
      }
    },
    toJSON = function() {
      ScientificNameObject <- list()
      if (!is.null(self$`fullScientificName`)) {
        ScientificNameObject[['fullScientificName']] <- self$`fullScientificName`
      }
      if (!is.null(self$`taxonomicStatus`)) {
        ScientificNameObject[['taxonomicStatus']] <- self$`taxonomicStatus`
      }
      if (!is.null(self$`genusOrMonomial`)) {
        ScientificNameObject[['genusOrMonomial']] <- self$`genusOrMonomial`
      }
      if (!is.null(self$`subgenus`)) {
        ScientificNameObject[['subgenus']] <- self$`subgenus`
      }
      if (!is.null(self$`specificEpithet`)) {
        ScientificNameObject[['specificEpithet']] <- self$`specificEpithet`
      }
      if (!is.null(self$`infraspecificEpithet`)) {
        ScientificNameObject[['infraspecificEpithet']] <- self$`infraspecificEpithet`
      }
      if (!is.null(self$`infraspecificMarker`)) {
        ScientificNameObject[['infraspecificMarker']] <- self$`infraspecificMarker`
      }
      if (!is.null(self$`nameAddendum`)) {
        ScientificNameObject[['nameAddendum']] <- self$`nameAddendum`
      }
      if (!is.null(self$`authorshipVerbatim`)) {
        ScientificNameObject[['authorshipVerbatim']] <- self$`authorshipVerbatim`
      }
      if (!is.null(self$`author`)) {
        ScientificNameObject[['author']] <- self$`author`
      }
      if (!is.null(self$`year`)) {
        ScientificNameObject[['year']] <- self$`year`
      }
      if (!is.null(self$`scientificNameGroup`)) {
        ScientificNameObject[['scientificNameGroup']] <- self$`scientificNameGroup`
      }
      if (!is.null(self$`references`)) {
        ScientificNameObject[['references']] <- lapply(self$`references`, function(x) x$toJSON())
      }
      if (!is.null(self$`experts`)) {
        ScientificNameObject[['experts']] <- lapply(self$`experts`, function(x) x$toJSON())
      }

      ScientificNameObject
    },
    fromJSON = function(ScientificNameJson) {
      ScientificNameObject <- jsonlite::fromJSON(ScientificNameJson)
      if (!is.null(ScientificNameObject$`fullScientificName`)) {
        self$`fullScientificName` <- ScientificNameObject$`fullScientificName`
      }
      if (!is.null(ScientificNameObject$`taxonomicStatus`)) {
        self$`taxonomicStatus` <- ScientificNameObject$`taxonomicStatus`
      }
      if (!is.null(ScientificNameObject$`genusOrMonomial`)) {
        self$`genusOrMonomial` <- ScientificNameObject$`genusOrMonomial`
      }
      if (!is.null(ScientificNameObject$`subgenus`)) {
        self$`subgenus` <- ScientificNameObject$`subgenus`
      }
      if (!is.null(ScientificNameObject$`specificEpithet`)) {
        self$`specificEpithet` <- ScientificNameObject$`specificEpithet`
      }
      if (!is.null(ScientificNameObject$`infraspecificEpithet`)) {
        self$`infraspecificEpithet` <- ScientificNameObject$`infraspecificEpithet`
      }
      if (!is.null(ScientificNameObject$`infraspecificMarker`)) {
        self$`infraspecificMarker` <- ScientificNameObject$`infraspecificMarker`
      }
      if (!is.null(ScientificNameObject$`nameAddendum`)) {
        self$`nameAddendum` <- ScientificNameObject$`nameAddendum`
      }
      if (!is.null(ScientificNameObject$`authorshipVerbatim`)) {
        self$`authorshipVerbatim` <- ScientificNameObject$`authorshipVerbatim`
      }
      if (!is.null(ScientificNameObject$`author`)) {
        self$`author` <- ScientificNameObject$`author`
      }
      if (!is.null(ScientificNameObject$`year`)) {
        self$`year` <- ScientificNameObject$`year`
      }
      if (!is.null(ScientificNameObject$`scientificNameGroup`)) {
        self$`scientificNameGroup` <- ScientificNameObject$`scientificNameGroup`
      }
      if (!is.null(ScientificNameObject$`references`)) {
        self$`references` <- lapply(ScientificNameObject$`references`, function(x) {
          referencesObject <- Reference$new()
          referencesObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          referencesObject
        })
      }
      if (!is.null(ScientificNameObject$`experts`)) {
        self$`experts` <- lapply(ScientificNameObject$`experts`, function(x) {
          expertsObject <- Expert$new()
          expertsObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          expertsObject
        })
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "fullScientificName": %s,
           "taxonomicStatus": %s,
           "genusOrMonomial": %s,
           "subgenus": %s,
           "specificEpithet": %s,
           "infraspecificEpithet": %s,
           "infraspecificMarker": %s,
           "nameAddendum": %s,
           "authorshipVerbatim": %s,
           "author": %s,
           "year": %s,
           "scientificNameGroup": %s,
           "references": [%s],
           "experts": [%s]
        }',
        self$`fullScientificName`,
        self$`taxonomicStatus`,
        self$`genusOrMonomial`,
        self$`subgenus`,
        self$`specificEpithet`,
        self$`infraspecificEpithet`,
        self$`infraspecificMarker`,
        self$`nameAddendum`,
        self$`authorshipVerbatim`,
        self$`author`,
        self$`year`,
        self$`scientificNameGroup`,
        lapply(self$`references`, function(x) paste(x$toJSON(), sep=",")),
        lapply(self$`experts`, function(x) paste(x$toJSON(), sep=","))
      )
    },
    fromJSONString = function(ScientificNameJson) {
      ScientificNameObject <- jsonlite::fromJSON(ScientificNameJson)
      self$`fullScientificName` <- ScientificNameObject$`fullScientificName`
      self$`taxonomicStatus` <- ScientificNameObject$`taxonomicStatus`
      self$`genusOrMonomial` <- ScientificNameObject$`genusOrMonomial`
      self$`subgenus` <- ScientificNameObject$`subgenus`
      self$`specificEpithet` <- ScientificNameObject$`specificEpithet`
      self$`infraspecificEpithet` <- ScientificNameObject$`infraspecificEpithet`
      self$`infraspecificMarker` <- ScientificNameObject$`infraspecificMarker`
      self$`nameAddendum` <- ScientificNameObject$`nameAddendum`
      self$`authorshipVerbatim` <- ScientificNameObject$`authorshipVerbatim`
      self$`author` <- ScientificNameObject$`author`
      self$`year` <- ScientificNameObject$`year`
      self$`scientificNameGroup` <- ScientificNameObject$`scientificNameGroup`
      self$`references` <- lapply(ScientificNameObject$`references`, function(x) Reference$new()$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE)))
      self$`experts` <- lapply(ScientificNameObject$`experts`, function(x) Expert$new()$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE)))
    }
  )
)
